<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>properties</title>

    <link href="{{C.BOOTSTRAP_CSS}}" rel="stylesheet">
	</head>
	<body>

		<div class="container">

			<ol class="breadcrumb">
				<li><a href="/{{or R.REF_SUFFIX ".xres-list"}}">HOME</a></li>
			{{#include "." "parents"}}
				<li><a href="{{path}}{{or R.REF_SUFFIX ".xres-list"}}">{{name}}</a></li>
			{{/include}}
				<li class="active">
					<a href="{{R.PATH}}{{or R.REF_SUFFIX ".xres-list"}}">{{R.NAME}}</a>
				</li>
			</ol>

		<h5>Other renderers</h5>
			<div class="form-horizontal">
					<form method="post" action="">
						<div class="form-group">
    			    <label class="col-sm-2">_rt</label>
							<div class="col-sm-8">
								<select class="form-control" name="_rt">
									<option value="">not set</option>
                  <option disabled>-</option>
							{{#include "/apps" "rendertypes"}}
									<option value="{{this}}" {{eq this ../renderType "selected"}}>{{this}}</option>
							{{/include}}
								</select>
							</div>
							<div class="col-sm-2">
								<button class="btn btn-success" type="submit">
                  <span>set</span>
								</button>
							</div>
						</div>
					</form>
          </div>

			<h5>properties:</h5>

				{{#include "." "properties"}}
			<div class="form-horizontal">
				<div class="form-group">
    			<label class="col-sm-2">{{name}}</label>
					<div class="col-sm-8">
						<input class="form-control" name="{{name}}" value="{{value}}" form="save_form" autocorrect="off" autocapitalize="off" autocomplete="off" spellcheck="false">
					</div>
					<div class="col-sm-2">
						<form method="POST" action="{{R.PATH}}{{R.SUFFIX}}">
							<input type="hidden" name="{{name}}" value="">
							<button class="btn btn-danger" type="submit" name="{{name}}" value="">
								<span class="glyphicon glyphicon-minus"></span>
							</button>
						</form>
					</div>
				</div>
				{{/include}}

				<form method="post" action="{{R.PATH}}{{R.SUFFIX}}">
					<div class="form-group">
						<div class="col-sm-2">
							<input class="form-control" name=":name" value="" autocorrect="off" autocapitalize="off" autocomplete="off" spellcheck="false">
						</div>
						<div class="col-sm-8">
							<input class="form-control" name="{:name}" value="" autocorrect="off" autocapitalize="off" autocomplete="off" spellcheck="false">
						</div>
						<div class="col-sm-2">
							<button class="btn btn-success" type="submit" value="add">
								<span class="glyphicon glyphicon-plus"></span>
							</button>
						</div>
					</div>
				</form>
			</div>

			<form method="post" id="save_form" action="{{R.PATH}}.xres-list">
				<input type="submit" value="save">
			</form>

		</div>

	</body>
</html>
