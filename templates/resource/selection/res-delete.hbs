<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
  <link rel="icon" href="data:;base64,iVBORw0KGgo=">

  {{include_css "_libs/ui.css"}}

  <title>delete:{{R.PATH}}</title>
</head>

<body class="ui_group ui_dialog">

  <h1 class="ui_label underlined">Delete "{{name}}"
    <a class="ui_button inline pull-right" href="{{req_path "." "ui-close"}}">X</a>
  </h1>

  <ol>
{{#include "." "keyvalues"}}
    <li>{{value}}</li>
    <input form="delete_sel" type="hidden" name=":delete:{{key}}" value="{{value}}">
{{/include}}
  </ol>

  <h4>Are you sure you want to delete the selection?

  <form id="delete_sel" method="post" action="{{res_path "."}}">
    <input type="hidden" name="_rt" value="{{renderType}}">
    <input type="hidden" name=":forward" value="{{req_path "." "ui-close"}}">
    <button type="submit">delete</button>
  </form>
  </h4>

</body>

</html>
