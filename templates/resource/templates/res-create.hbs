<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" href="data:;base64,iVBORw0KGgo=">

  <title>create</title>

  <link href="{{C.BOOTSTRAP_CSS}}" rel="stylesheet">
</head>

<body>
  
  <div role="dialog">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <a class="close" href="{{C.P}}{{R.PATH_APPEND}}../{{C.X}}res-list"><span aria-hidden="true">&times;</span></a>
          <h4 class="modal-title">Create New Template</h4>
        </div>
        <div class="modal-body">

    <ol class="breadcrumb">
      <li class="active">HOME</li>
      {{#include "." "parents"}}
        <li class="active">{{name}}</li>
      {{/include}}
      <li class="active">{{R.NAME}}</li>
    </ol>

    <h6>create renderer:</h6>

        <div>
          <p>
            Renderer templates are resolved by scanning for a file matching specific naming convention pattern.
          <p>
            Pattern is composed of these parts: <br>
            <span class="bg-info">resource type</span> - usually a directory<br>
            <span class="bg-success">selector</span> - "default" or what gets appended after "{{C.X}}" like "json" for {{C.X}}json
          <p>
            There are two pattern variants you can use, for example:
          <ul>
            <li><span class="bg-info">web/mycomponent</span><b>/</b><span class="bg-success">default</span></li>
            <li><span class="bg-info">web/mycomponent</span>/mycomponent<b>.</b><span class="bg-success">default</span></li>
          </ul>
        </div>

    <div class="form-horizontal">
      <form method="post" action="{{C.P}}{{R.PATH}}/{:name}.{:ext}">
        <div class="form-group">
            <div class="col-sm-3">
          <select name=":ext" class="form-control">
            <option value="hbs">
              handlebars
            </option>
            <option value="js">
              javascript
            </option>
          </select>
          </div>


            <div class="col-sm-7">
          <input class="form-control" name=":name" value="{{R.DATA_PATH}}" placeholder="full template file path" required="" autocomplete="off">
          <input type="hidden" name="_content" value=""> 
          <input type="hidden" name="_rt" value="resource/content"> 
          <input type="hidden" name=":forward" value="{{C.P}}{{R.PATH}}/{:name}.{:ext}{{C.X}}edit">
          </div>

            <div class="col-sm-2">
                <button class="btn btn-success" type="submit" value="add">
                  <span class="glyphicon glyphicon-plus"></span>
                </button>
                </div>
        </div>
      </form>
    </div>

    <h6>create directory:</h6>

      <div class="form-horizontal">
        <form method="post" action="{{C.P}}{{R.PATH}}/{:name}">
          <div class="form-group">
            <div class="col-sm-8">
              <input class="form-control" name=":name" value="" required="" autocomplete="off"> <input type="hidden" name=":forward" value="{{C.P}}{{R.PATH}}{{C.X}}res-list">
            </div>


            <div class="col-sm-2">
                <button class="btn btn-success" type="submit" value="add">
                  <span class="glyphicon glyphicon-plus"></span>
                </button>
            </div>
          </div>
        </form>
    </div>

        </div>
        <div class="modal-footer">
          <a class="btn btn-default" href="{{C.P}}{{R.PATH}}{{C.X}}res-list">Cancel</a>
        </div>
      </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
  </div><!-- /.modal -->

</body>

</html>
