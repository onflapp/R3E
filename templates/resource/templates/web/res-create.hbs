<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" href="data:;base64,iVBORw0KGgo=">

  <title>create</title>
</head>

<body>
  
  <ol>
    <li>HOME</li>
{{#include "." "parents"}}
    <li>{{name}}</li>
{{/include}}
    <li>{{R.NAME}}</li>
  </ol>

  <h6>create renderer:</h6>

  <p>
    Renderer templates are resolved by scanning for a file matching specific naming convention pattern.
  <p>
    Pattern is composed of these parts: <br>
            <span class="bg-info">resource type</span> - usually a directory<br>
            <span class="bg-success">selector</span> - "default" or what gets appended after "{{C.X}}" like "json" for {{C.X}}json
          <p>
            There are two pattern variants you can use, for example:
          <ul>
            <li><span class="bg-info">web/mycomponent</span><b>/</b><span class="bg-success">default</span></li>
            <li><span class="bg-info">web/mycomponent</span>/mycomponent<b>.</b><span class="bg-success">default</span></li>
          </ul>

  <form method="POST" action="{{R.PATH}}/{:name}.{:ext}">
    <select name=":ext">
      <option value="hbs">handlebars</option>
      <option value="js">javascript</option>
    </select>

    <input name=":name" value="{{R.DATA_PATH}}" placeholder="full template file path" required="" autocomplete="off">
    <input type="hidden" name="_content" value=""> 
    <input type="hidden" name="_rt" value="resource/content"> 
    <input type="hidden" name=":forward" value="{{R.PATH}}/{:name}.{:ext}{{C.X}}edit">

    <button type="submit" value="add">add</button>
  </form>

  <h6>create directory:</h6>

  <form method="POST" action="{{R.PATH}}/{:name}">
    <input name=":name" value="" required="" autocomplete="off"> <input type="hidden" name=":forward" value="{{C.P}}{{R.PATH}}{{C.X}}res-list">

    <button class="btn btn-success" type="submit" value="add">add</button>
  </form>

  <a class="btn btn-default" href="{{req_path "." "res-list"}}">Cancel</a>

</body>

</html>
